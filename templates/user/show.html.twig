{% extends 'app.html.twig' %}

{% block title %}
    {{ 'title.show.user'|trans(locale = userLocate) }}
{% endblock %}

{% block pageName %}
    {{ 'title.show.user'|trans(locale = userLocate) }}
{% endblock %}

{% block breadcrumbName %}
    <li class="breadcrumb-item">
    {{ 'title.user'|trans(locale = userLocate) }}
    </li>
    <li class="breadcrumb-item">
    {{ 'title.show.user'|trans(locale = userLocate) }}
    </li>
    <li class="breadcrumb-item active active">
        {{ user.nomeCompleto }}
    </li>
{% endblock %}

{% block content %}

    <div class="row">
        <div class="col-12 d-flex justify-content-end">
            <a href="{{ path('app_user_index') }}" class="btn btn-danger btn-sm text-white mb-2 shadow">
                <i class="fa-solid fa-left-long"></i>
                {{ 'grid.options.voltar'|trans(locale = userLocate) }}
            </a>
        </div>
    </div>

    <div class="row">
       <div class="col-12">
            <div class="card border-0 mb-5 shadow">
                <div class="card-body">

                    {{ include('user/_entity.html.twig') }}

                </div>
            </div>
        </div>
    </div>
    
{% endblock %}
