<form method="post" action="{{ path('app_lancamento_baixar_lancamento', {'id': id}) }}" onsubmit="return confirm('Tem certeza de que deseja baixar esse lançamento?');">
    <input type="hidden" name="_token" value="{{ csrf_token('baixar' ~ id) }}">
    <button class="btn btn-info btn-sm text-white shadow-sm">
        <i class="fa-solid fa-dollar-sign"></i> Baixar
    </button>
</form>